import { PerspectiveCamera as PerspectiveCameraThree } from 'three';
import { CameraParams } from '../../types';

export declare class PerspectiveCamera extends PerspectiveCameraThree {
    constructor(params: CameraParams);
}
